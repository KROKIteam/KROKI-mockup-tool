<#if panels??>
<#if panels?has_content>
{
    "panels": [
        <#list panels as panel>
        {
            "activate"          : "${panel.name}"<#if (panel.associationEnd??)>,
            "assoiciation_end"  : "${panel.associationEnd}"</#if>

        } <#if (panel_has_next)>,</#if>
        </#list>
    ]
}
</#if>
</#if>