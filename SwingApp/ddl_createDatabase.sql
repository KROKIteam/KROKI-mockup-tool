
    drop table DSF_PANEL if exists;

    drop table DSF_USER if exists;

    create table DSF_PANEL (
        ID bigint generated by default as identity unique,
        PAN_SDFDS varchar(255) not null,
        primary key (ID)
    );

    create table DSF_USER (
        ID bigint generated by default as identity unique,
        password varchar(255) not null unique,
        username varchar(255) not null unique,
        primary key (ID)
    );
